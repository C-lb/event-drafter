{
  "name": "@event-drafter/desktop",
  "version": "0.0.1",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "electron .",
    "test": "vitest run",
    "rebuild-native": "electron-rebuild -f -w better-sqlite3",
    "fetch-browser": "node scripts/fetch-browser.mjs",
    "dist": "npm run build && electron-builder --publish never"
  },
  "devDependencies": {
    "electron": "33.4.11",
    "@electron/rebuild": "^3.7.0",
    "electron-builder": "^25.1.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0"
  }
}
