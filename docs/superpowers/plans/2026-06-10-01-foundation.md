# Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up the empty-but-runnable skeleton: monorepo, two processes (web + worker), shared core package with SQLite + Drizzle migrated to all 7 tables, a one-command dev runner. Nothing functional yet; everything else builds on this.

**Architecture:** npm workspaces monorepo. `packages/core` exports the Drizzle schema + a shared `getDb()` client. `packages/web` is a Next.js 16 App Router app. `packages/worker` is a long-lived Node process that polls the `jobs` table. Both processes open the same SQLite file at `data/app.db`. Dev runner uses `concurrently` to start both.

**Tech Stack:** Node 22, TypeScript (ESM), npm workspaces, Next.js 16 + React 19, Drizzle ORM, `better-sqlite3`, Vitest, `concurrently`, `tsx` for worker dev.

---

## File structure produced by this plan

```
vip-event-drafter/
├── package.json                          # root, npm workspaces, dev script
├── tsconfig.base.json                    # shared TS config
├── .nvmrc                                # node 22
├── packages/
│   ├── core/
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── drizzle.config.ts
│   │   ├── src/
│   │   │   ├── index.ts                  # public exports
│   │   │   ├── db.ts                     # getDb(), better-sqlite3 + drizzle
│   │   │   ├── schema/
│   │   │   │   ├── index.ts              # re-exports all tables
│   │   │   │   ├── contacts.ts
│   │   │   │   ├── events.ts
│   │   │   │   ├── invites.ts
│   │   │   │   ├── replies.ts
│   │   │   │   ├── follow-ups.ts
│   │   │   │   ├── jobs.ts
│   │   │   │   └── wa-chat-cursors.ts
│   │   │   ├── migrate.ts                # CLI: applies migrations on startup
│   │   │   └── types.ts                  # JobKind, InviteStatus, etc.
│   │   ├── drizzle/                      # generated migration SQL
│   │   └── test/
│   │       └── schema.test.ts            # smoke: open DB, insert+select per table
│   ├── web/
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── next.config.ts
│   │   ├── tailwind.config.ts
│   │   ├── postcss.config.mjs
│   │   ├── app/
│   │   │   ├── layout.tsx
│   │   │   ├── page.tsx                  # dashboard home (empty for now)
│   │   │   └── globals.css
│   │   └── lib/db.ts                     # re-exports getDb() from core
│   └── worker/
│       ├── package.json
│       ├── tsconfig.json
│       ├── src/
│       │   ├── index.ts                  # entry: migrate, then start poller
│       │   ├── poller.ts                 # job loop: select queued, dispatch
│       │   ├── jobs/
│       │   │   ├── index.ts              # JobHandlers map
│       │   │   └── noop.ts               # placeholder handler used by all kinds
│       │   └── logger.ts                 # structured console logger
│       └── test/
│           └── poller.test.ts            # enqueue + tick + assert handler ran
├── data/                                  # gitignored; created on first run
└── README.md
```

---

## Task 1: Initialize root package + workspaces

**Files:**
- Create: `package.json`
- Create: `tsconfig.base.json`
- Create: `.nvmrc`
- Modify: `.gitignore` (add a few lines)
- Create: `README.md`

- [ ] **Step 1: Write `.nvmrc`**

```
22
```

- [ ] **Step 2: Write root `package.json`**

```json
{
  "name": "vip-event-drafter",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "concurrently -k -n web,worker -c blue,magenta \"npm -w @vip/web run dev\" \"npm -w @vip/worker run dev\"",
    "build": "npm -w @vip/core run build && npm -w @vip/web run build && npm -w @vip/worker run build",
    "test": "npm -ws --if-present run test",
    "migrate": "npm -w @vip/core run migrate",
    "lint": "npm -ws --if-present run lint"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "^5.6.0"
  }
}
```

- [ ] **Step 3: Write `tsconfig.base.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": false,
    "declaration": true,
    "sourceMap": true
  }
}
```

- [ ] **Step 4: Append to `.gitignore`**

Add these lines (some may already exist; ensure all present without duplicates):

```
node_modules/
data/
.env
.env.local
.DS_Store
dist/
.next/
*.log
*.tsbuildinfo
playwright-report/
test-results/
```

- [ ] **Step 5: Write `README.md`**

```markdown
# vip-event-drafter

Local single-user tool to draft and track personalized WhatsApp invites for VIP contacts.

See `docs/superpowers/specs/2026-06-10-vip-event-drafter-design.md` for the design.

## Run

```bash
nvm use            # node 22
npm install
npm run migrate    # initialize SQLite schema in ./data/app.db
npm run dev        # starts web (localhost:3000) + worker concurrently
```
```

- [ ] **Step 6: Install root deps**

Run: `npm install`
Expected: creates `node_modules/`, no errors.

- [ ] **Step 7: Commit**

```bash
git add package.json tsconfig.base.json .nvmrc .gitignore README.md package-lock.json
git commit -m "chore: monorepo scaffolding (workspaces, tsconfig, dev runner)"
```

---

## Task 2: Create `@vip/core` package skeleton

**Files:**
- Create: `packages/core/package.json`
- Create: `packages/core/tsconfig.json`
- Create: `packages/core/src/index.ts`

- [ ] **Step 1: Write `packages/core/package.json`**

```json
{
  "name": "@vip/core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./schema": "./dist/schema/index.js",
    "./db": "./dist/db.js",
    "./types": "./dist/types.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "migrate": "tsx src/migrate.ts",
    "drizzle:generate": "drizzle-kit generate"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.28.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}
```

- [ ] **Step 2: Write `packages/core/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3: Write `packages/core/src/index.ts`** (stub for now)

```ts
export * from './schema/index.js';
export * from './db.js';
export * from './types.js';
```

- [ ] **Step 4: Install workspace deps**

Run: `npm install`
Expected: `@vip/core` deps installed under root `node_modules/`.

- [ ] **Step 5: Commit**

```bash
git add packages/core/package.json packages/core/tsconfig.json packages/core/src/index.ts package.json package-lock.json
git commit -m "feat(core): package skeleton"
```

---

## Task 3: Define shared types

**Files:**
- Create: `packages/core/src/types.ts`

- [ ] **Step 1: Write `packages/core/src/types.ts`**

```ts
export const JOB_KINDS = [
  'send_message',
  'check_replies',
  'classify_reply',
  'draft_response',
  'generate_follow_up',
] as const;
export type JobKind = (typeof JOB_KINDS)[number];

export const JOB_STATUSES = ['queued', 'running', 'succeeded', 'failed'] as const;
export type JobStatus = (typeof JOB_STATUSES)[number];

export const INVITE_STATUSES = [
  'pending',
  'drafted',
  'approved',
  'sent',
  'skipped',
  'failed',
] as const;
export type InviteStatus = (typeof INVITE_STATUSES)[number];

export const RSVP_VALUES = ['yes', 'no', 'maybe', 'none'] as const;
export type Rsvp = (typeof RSVP_VALUES)[number];

export const REPLY_CLASSIFICATIONS = ['yes', 'no', 'maybe', 'unclear'] as const;
export type ReplyClassification = (typeof REPLY_CLASSIFICATIONS)[number];

export const EVENT_STATUSES = ['draft', 'drafting', 'sending', 'closed'] as const;
export type EventStatus = (typeof EVENT_STATUSES)[number];

export const FOLLOW_UP_STATUSES = [
  'pending',
  'drafted',
  'approved',
  'sent',
  'skipped',
  'failed',
] as const;
export type FollowUpStatus = (typeof FOLLOW_UP_STATUSES)[number];

export const RESPONSE_STATUSES = [
  'pending',
  'drafted',
  'approved',
  'sent',
  'skipped',
  'failed',
] as const;
export type ResponseStatus = (typeof RESPONSE_STATUSES)[number];
```

- [ ] **Step 2: Commit**

```bash
git add packages/core/src/types.ts
git commit -m "feat(core): shared union types for job/invite/reply states"
```

---

## Task 4: Drizzle config + db client

**Files:**
- Create: `packages/core/drizzle.config.ts`
- Create: `packages/core/src/db.ts`

- [ ] **Step 1: Write `packages/core/drizzle.config.ts`**

```ts
import type { Config } from 'drizzle-kit';

export default {
  schema: './src/schema/index.ts',
  out: './drizzle',
  dialect: 'sqlite',
  dbCredentials: {
    url: process.env.VIP_DB_PATH ?? '../../data/app.db',
  },
} satisfies Config;
```

- [ ] **Step 2: Write `packages/core/src/db.ts`**

```ts
import Database from 'better-sqlite3';
import { drizzle, type BetterSQLite3Database } from 'drizzle-orm/better-sqlite3';
import * as schema from './schema/index.js';
import { mkdirSync } from 'node:fs';
import { dirname, resolve } from 'node:path';

export type DB = BetterSQLite3Database<typeof schema>;

let _db: DB | null = null;
let _sqlite: Database.Database | null = null;

export function getDbPath(): string {
  return process.env.VIP_DB_PATH ?? resolve(process.cwd(), 'data/app.db');
}

export function getDb(): DB {
  if (_db) return _db;
  const path = getDbPath();
  mkdirSync(dirname(path), { recursive: true });
  _sqlite = new Database(path);
  _sqlite.pragma('journal_mode = WAL');
  _sqlite.pragma('foreign_keys = ON');
  _db = drizzle(_sqlite, { schema });
  return _db;
}

export function closeDb(): void {
  if (_sqlite) {
    _sqlite.close();
    _sqlite = null;
    _db = null;
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add packages/core/drizzle.config.ts packages/core/src/db.ts
git commit -m "feat(core): drizzle + better-sqlite3 client with WAL"
```

---

## Task 5: Schema — contacts table

**Files:**
- Create: `packages/core/src/schema/contacts.ts`

- [ ] **Step 1: Write `packages/core/src/schema/contacts.ts`**

```ts
import { sqliteTable, integer, text, uniqueIndex } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';

export const contacts = sqliteTable(
  'contacts',
  {
    id: integer('id').primaryKey({ autoIncrement: true }),
    full_name: text('full_name').notNull(),
    preferred_name: text('preferred_name'),
    phone_e164: text('phone_e164').notNull(),
    email: text('email'),
    personal_note: text('personal_note'),
    interests: text('interests'),
    relationship_notes: text('relationship_notes'),
    sheet_row_hash: text('sheet_row_hash'),
    created_at: integer('created_at', { mode: 'timestamp_ms' })
      .notNull()
      .default(sql`(unixepoch() * 1000)`),
    updated_at: integer('updated_at', { mode: 'timestamp_ms' })
      .notNull()
      .default(sql`(unixepoch() * 1000)`),
  },
  (t) => ({
    phoneIdx: uniqueIndex('contacts_phone_idx').on(t.phone_e164),
  }),
);

export type Contact = typeof contacts.$inferSelect;
export type NewContact = typeof contacts.$inferInsert;
```

- [ ] **Step 2: Commit**

```bash
git add packages/core/src/schema/contacts.ts
git commit -m "feat(core): contacts schema"
```

---

## Task 6: Schema — events table

**Files:**
- Create: `packages/core/src/schema/events.ts`

- [ ] **Step 1: Write `packages/core/src/schema/events.ts`**

```ts
import { sqliteTable, integer, text } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';
import type { EventStatus } from '../types.js';

export const events = sqliteTable('events', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  name: text('name').notNull(),
  event_date: integer('event_date', { mode: 'timestamp_ms' }).notNull(),
  venue: text('venue'),
  edm_subject: text('edm_subject'),
  edm_body: text('edm_body'),
  gmail_message_id: text('gmail_message_id'),
  status: text('status').notNull().$type<EventStatus>().default('draft'),
  created_at: integer('created_at', { mode: 'timestamp_ms' })
    .notNull()
    .default(sql`(unixepoch() * 1000)`),
});

export type Event = typeof events.$inferSelect;
export type NewEvent = typeof events.$inferInsert;
```

- [ ] **Step 2: Commit**

```bash
git add packages/core/src/schema/events.ts
git commit -m "feat(core): events schema"
```

---

## Task 7: Schema — invites table

**Files:**
- Create: `packages/core/src/schema/invites.ts`

- [ ] **Step 1: Write `packages/core/src/schema/invites.ts`**

```ts
import { sqliteTable, integer, text, uniqueIndex } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';
import type { InviteStatus, Rsvp } from '../types.js';
import { events } from './events.js';
import { contacts } from './contacts.js';

export const invites = sqliteTable(
  'invites',
  {
    id: integer('id').primaryKey({ autoIncrement: true }),
    event_id: integer('event_id')
      .notNull()
      .references(() => events.id, { onDelete: 'cascade' }),
    contact_id: integer('contact_id')
      .notNull()
      .references(() => contacts.id, { onDelete: 'cascade' }),
    draft_text: text('draft_text'),
    draft_generated_at: integer('draft_generated_at', { mode: 'timestamp_ms' }),
    approved_at: integer('approved_at', { mode: 'timestamp_ms' }),
    sent_at: integer('sent_at', { mode: 'timestamp_ms' }),
    status: text('status').notNull().$type<InviteStatus>().default('pending'),
    rsvp: text('rsvp').notNull().$type<Rsvp>().default('none'),
    attended: integer('attended', { mode: 'boolean' }).notNull().default(false),
    attended_notes: text('attended_notes'),
    generation_meta: text('generation_meta', { mode: 'json' }).$type<Record<string, unknown>>(),
    created_at: integer('created_at', { mode: 'timestamp_ms' })
      .notNull()
      .default(sql`(unixepoch() * 1000)`),
  },
  (t) => ({
    eventContactIdx: uniqueIndex('invites_event_contact_idx').on(t.event_id, t.contact_id),
  }),
);

export type Invite = typeof invites.$inferSelect;
export type NewInvite = typeof invites.$inferInsert;
```

- [ ] **Step 2: Commit**

```bash
git add packages/core/src/schema/invites.ts
git commit -m "feat(core): invites schema"
```

---

## Task 8: Schema — replies, follow_ups, jobs, wa_chat_cursors

**Files:**
- Create: `packages/core/src/schema/replies.ts`
- Create: `packages/core/src/schema/follow-ups.ts`
- Create: `packages/core/src/schema/jobs.ts`
- Create: `packages/core/src/schema/wa-chat-cursors.ts`
- Create: `packages/core/src/schema/index.ts`

- [ ] **Step 1: Write `packages/core/src/schema/replies.ts`**

```ts
import { sqliteTable, integer, text, real } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';
import type { ReplyClassification, ResponseStatus } from '../types.js';
import { invites } from './invites.js';

export const replies = sqliteTable('replies', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  invite_id: integer('invite_id')
    .notNull()
    .references(() => invites.id, { onDelete: 'cascade' }),
  wa_message_text: text('wa_message_text').notNull(),
  wa_sent_at: integer('wa_sent_at', { mode: 'timestamp_ms' }).notNull(),
  detected_at: integer('detected_at', { mode: 'timestamp_ms' })
    .notNull()
    .default(sql`(unixepoch() * 1000)`),
  classification: text('classification').$type<ReplyClassification>(),
  classification_confidence: real('classification_confidence'),
  classification_summary: text('classification_summary'),
  response_draft: text('response_draft'),
  response_approved_at: integer('response_approved_at', { mode: 'timestamp_ms' }),
  response_sent_at: integer('response_sent_at', { mode: 'timestamp_ms' }),
  response_status: text('response_status').$type<ResponseStatus>().default('pending'),
});

export type Reply = typeof replies.$inferSelect;
export type NewReply = typeof replies.$inferInsert;
```

- [ ] **Step 2: Write `packages/core/src/schema/follow-ups.ts`**

```ts
import { sqliteTable, integer, text } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';
import type { FollowUpStatus } from '../types.js';
import { invites } from './invites.js';

export const follow_ups = sqliteTable('follow_ups', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  invite_id: integer('invite_id')
    .notNull()
    .references(() => invites.id, { onDelete: 'cascade' }),
  draft_text: text('draft_text').notNull(),
  generated_at: integer('generated_at', { mode: 'timestamp_ms' })
    .notNull()
    .default(sql`(unixepoch() * 1000)`),
  approved_at: integer('approved_at', { mode: 'timestamp_ms' }),
  sent_at: integer('sent_at', { mode: 'timestamp_ms' }),
  status: text('status').notNull().$type<FollowUpStatus>().default('pending'),
});

export type FollowUp = typeof follow_ups.$inferSelect;
export type NewFollowUp = typeof follow_ups.$inferInsert;
```

- [ ] **Step 3: Write `packages/core/src/schema/jobs.ts`**

```ts
import { sqliteTable, integer, text, index } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';
import type { JobKind, JobStatus } from '../types.js';

export const jobs = sqliteTable(
  'jobs',
  {
    id: integer('id').primaryKey({ autoIncrement: true }),
    kind: text('kind').notNull().$type<JobKind>(),
    payload: text('payload', { mode: 'json' }).notNull().$type<Record<string, unknown>>(),
    status: text('status').notNull().$type<JobStatus>().default('queued'),
    attempts: integer('attempts').notNull().default(0),
    last_error: text('last_error'),
    run_after: integer('run_after', { mode: 'timestamp_ms' }),
    created_at: integer('created_at', { mode: 'timestamp_ms' })
      .notNull()
      .default(sql`(unixepoch() * 1000)`),
    started_at: integer('started_at', { mode: 'timestamp_ms' }),
    finished_at: integer('finished_at', { mode: 'timestamp_ms' }),
  },
  (t) => ({
    statusRunAfterIdx: index('jobs_status_runafter_idx').on(t.status, t.run_after),
  }),
);

export type Job = typeof jobs.$inferSelect;
export type NewJob = typeof jobs.$inferInsert;
```

- [ ] **Step 4: Write `packages/core/src/schema/wa-chat-cursors.ts`**

```ts
import { sqliteTable, integer } from 'drizzle-orm/sqlite-core';
import { sql } from 'drizzle-orm';
import { contacts } from './contacts.js';

export const wa_chat_cursors = sqliteTable('wa_chat_cursors', {
  contact_id: integer('contact_id')
    .primaryKey()
    .references(() => contacts.id, { onDelete: 'cascade' }),
  last_seen_wa_sent_at: integer('last_seen_wa_sent_at', { mode: 'timestamp_ms' }).notNull(),
  updated_at: integer('updated_at', { mode: 'timestamp_ms' })
    .notNull()
    .default(sql`(unixepoch() * 1000)`),
});

export type WaChatCursor = typeof wa_chat_cursors.$inferSelect;
export type NewWaChatCursor = typeof wa_chat_cursors.$inferInsert;
```

- [ ] **Step 5: Write `packages/core/src/schema/index.ts`**

```ts
export * from './contacts.js';
export * from './events.js';
export * from './invites.js';
export * from './replies.js';
export * from './follow-ups.js';
export * from './jobs.js';
export * from './wa-chat-cursors.js';
```

- [ ] **Step 6: Commit**

```bash
git add packages/core/src/schema/
git commit -m "feat(core): remaining schema (replies, follow_ups, jobs, wa_chat_cursors)"
```

---

## Task 9: Generate initial migration

**Files:**
- Create: `packages/core/drizzle/0000_*.sql` (generated)

- [ ] **Step 1: Generate migration**

Run: `cd packages/core && npx drizzle-kit generate --name init`
Expected: writes `packages/core/drizzle/0000_init.sql` and `packages/core/drizzle/meta/_journal.json`.

- [ ] **Step 2: Inspect the generated SQL**

Open `packages/core/drizzle/0000_init.sql`. Verify it contains `CREATE TABLE` for all seven tables and the unique indexes for `contacts_phone_idx`, `invites_event_contact_idx`, and `jobs_status_runafter_idx`.

If any table is missing or has wrong columns, fix the corresponding `packages/core/src/schema/*.ts` file and regenerate.

- [ ] **Step 3: Commit**

```bash
git add packages/core/drizzle/
git commit -m "feat(core): initial migration"
```

---

## Task 10: Migration runner

**Files:**
- Create: `packages/core/src/migrate.ts`

- [ ] **Step 1: Write `packages/core/src/migrate.ts`**

```ts
import { migrate } from 'drizzle-orm/better-sqlite3/migrator';
import { getDb, closeDb } from './db.js';
import { resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));

export function runMigrations(): void {
  const db = getDb();
  const migrationsFolder = resolve(__dirname, '../drizzle');
  migrate(db, { migrationsFolder });
}

if (import.meta.url === `file://${process.argv[1]}`) {
  try {
    runMigrations();
    console.log('migrations applied');
  } finally {
    closeDb();
  }
}
```

- [ ] **Step 2: Run migration from repo root**

Run: `npm run migrate`
Expected: prints `migrations applied`, creates `data/app.db`.

- [ ] **Step 3: Verify tables exist**

Run: `sqlite3 data/app.db ".tables"`
Expected output (order may vary):
```
__drizzle_migrations  events                invites
contacts              follow_ups            jobs
replies               wa_chat_cursors
```

- [ ] **Step 4: Commit**

```bash
git add packages/core/src/migrate.ts
git commit -m "feat(core): migration runner CLI"
```

---

## Task 11: Smoke test — insert + select per table

**Files:**
- Create: `packages/core/test/schema.test.ts`
- Create: `packages/core/vitest.config.ts`

- [ ] **Step 1: Write `packages/core/vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    pool: 'forks',
    poolOptions: { forks: { singleFork: true } },
  },
});
```

- [ ] **Step 2: Write `packages/core/test/schema.test.ts`**

```ts
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { mkdtempSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { runMigrations } from '../src/migrate.js';
import { closeDb, getDb } from '../src/db.js';
import { contacts, events, invites, replies, follow_ups, jobs, wa_chat_cursors } from '../src/schema/index.js';
import { eq } from 'drizzle-orm';

let tmp: string;

beforeEach(() => {
  tmp = mkdtempSync(join(tmpdir(), 'vip-test-'));
  process.env.VIP_DB_PATH = join(tmp, 'app.db');
  runMigrations();
});

afterEach(() => {
  closeDb();
  rmSync(tmp, { recursive: true, force: true });
});

describe('schema smoke', () => {
  it('inserts and selects a contact', () => {
    const db = getDb();
    db.insert(contacts).values({ full_name: 'Ada Lovelace', phone_e164: '+6512345678' }).run();
    const rows = db.select().from(contacts).all();
    expect(rows).toHaveLength(1);
    expect(rows[0]?.full_name).toBe('Ada Lovelace');
  });

  it('enforces unique phone_e164', () => {
    const db = getDb();
    db.insert(contacts).values({ full_name: 'A', phone_e164: '+6500000000' }).run();
    expect(() =>
      db.insert(contacts).values({ full_name: 'B', phone_e164: '+6500000000' }).run(),
    ).toThrow();
  });

  it('cascades invite delete from event', () => {
    const db = getDb();
    const c = db.insert(contacts).values({ full_name: 'A', phone_e164: '+651' }).returning().get();
    const e = db.insert(events).values({ name: 'Gala', event_date: new Date() }).returning().get();
    db.insert(invites).values({ event_id: e.id, contact_id: c.id }).run();
    db.delete(events).where(eq(events.id, e.id)).run();
    expect(db.select().from(invites).all()).toHaveLength(0);
  });

  it('enforces UNIQUE (event_id, contact_id) on invites', () => {
    const db = getDb();
    const c = db.insert(contacts).values({ full_name: 'A', phone_e164: '+652' }).returning().get();
    const e = db.insert(events).values({ name: 'Gala', event_date: new Date() }).returning().get();
    db.insert(invites).values({ event_id: e.id, contact_id: c.id }).run();
    expect(() =>
      db.insert(invites).values({ event_id: e.id, contact_id: c.id }).run(),
    ).toThrow();
  });

  it('inserts into every remaining table without error', () => {
    const db = getDb();
    const c = db.insert(contacts).values({ full_name: 'A', phone_e164: '+653' }).returning().get();
    const e = db.insert(events).values({ name: 'Gala', event_date: new Date() }).returning().get();
    const i = db.insert(invites).values({ event_id: e.id, contact_id: c.id }).returning().get();
    db.insert(replies).values({ invite_id: i.id, wa_message_text: 'yes', wa_sent_at: new Date() }).run();
    db.insert(follow_ups).values({ invite_id: i.id, draft_text: 'hi again' }).run();
    db.insert(jobs).values({ kind: 'send_message', payload: { invite_id: i.id } }).run();
    db.insert(wa_chat_cursors).values({ contact_id: c.id, last_seen_wa_sent_at: new Date() }).run();
    expect(db.select().from(jobs).all()).toHaveLength(1);
  });
});
```

- [ ] **Step 3: Run tests**

Run: `npm -w @vip/core run test`
Expected: 5 tests pass.

- [ ] **Step 4: Commit**

```bash
git add packages/core/test/schema.test.ts packages/core/vitest.config.ts
git commit -m "test(core): schema smoke (insert, unique, cascade)"
```

---

## Task 12: Build the core package

**Files:**
- (none new; verifies build)

- [ ] **Step 1: Build core**

Run: `npm -w @vip/core run build`
Expected: emits `packages/core/dist/` with `.js` and `.d.ts` files. No errors.

- [ ] **Step 2: Commit if any tsconfig tweaks were needed**

(If build is clean, no commit.)

---

## Task 13: Scaffold `@vip/web` (Next.js 16)

**Files:**
- Create: `packages/web/package.json`
- Create: `packages/web/tsconfig.json`
- Create: `packages/web/next.config.ts`
- Create: `packages/web/postcss.config.mjs`
- Create: `packages/web/tailwind.config.ts`
- Create: `packages/web/app/layout.tsx`
- Create: `packages/web/app/page.tsx`
- Create: `packages/web/app/globals.css`
- Create: `packages/web/lib/db.ts`

- [ ] **Step 1: Write `packages/web/package.json`**

```json
{
  "name": "@vip/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev --port 3000",
    "build": "next build",
    "start": "next start --port 3000",
    "lint": "next lint"
  },
  "dependencies": {
    "@vip/core": "*",
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.0"
  }
}
```

- [ ] **Step 2: Write `packages/web/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

- [ ] **Step 3: Write `packages/web/next.config.ts`**

```ts
import type { NextConfig } from 'next';

const config: NextConfig = {
  transpilePackages: ['@vip/core'],
  experimental: {
    serverActions: { bodySizeLimit: '2mb' },
  },
};

export default config;
```

- [ ] **Step 4: Write `packages/web/postcss.config.mjs`**

```js
export default {
  plugins: { '@tailwindcss/postcss': {} },
};
```

(Note: Tailwind v4 uses the `@tailwindcss/postcss` plugin. If `npm install` fails because of this name, fall back to `tailwindcss: {}` with v3 — adjust both this file and the `package.json` deps.)

- [ ] **Step 5: Write `packages/web/tailwind.config.ts`**

```ts
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./app/**/*.{ts,tsx}'],
  theme: { extend: {} },
  plugins: [],
};

export default config;
```

- [ ] **Step 6: Write `packages/web/app/globals.css`**

```css
@import "tailwindcss";

body {
  font-family: -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
}
```

- [ ] **Step 7: Write `packages/web/app/layout.tsx`**

```tsx
import './globals.css';
import type { ReactNode } from 'react';

export const metadata = { title: 'VIP Event Drafter' };

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="en">
      <body className="min-h-screen bg-neutral-50 text-neutral-900">
        <header className="border-b border-neutral-200 px-6 py-3">
          <h1 className="text-lg font-semibold">VIP Event Drafter</h1>
        </header>
        <main className="px-6 py-6">{children}</main>
      </body>
    </html>
  );
}
```

- [ ] **Step 8: Write `packages/web/app/page.tsx`**

```tsx
import { getDb } from '@/lib/db';
import { jobs } from '@vip/core/schema';
import { sql } from 'drizzle-orm';

export const dynamic = 'force-dynamic';

export default async function HomePage() {
  const db = getDb();
  const [{ count }] = db
    .select({ count: sql<number>`count(*)` })
    .from(jobs)
    .all();
  return (
    <section>
      <p className="text-sm text-neutral-600">
        Foundation scaffold. <code>jobs</code> table row count: {count ?? 0}.
      </p>
    </section>
  );
}
```

- [ ] **Step 9: Write `packages/web/lib/db.ts`**

```ts
export { getDb, closeDb, getDbPath } from '@vip/core/db';
```

- [ ] **Step 10: Install + run dev**

Run from repo root:
```
npm install
npm run migrate
npm -w @vip/web run dev
```

Visit `http://localhost:3000`.
Expected: page renders showing "Foundation scaffold. jobs table row count: 0."

Stop with Ctrl-C.

- [ ] **Step 11: Commit**

```bash
git add packages/web/ package.json package-lock.json
git commit -m "feat(web): Next.js 16 dashboard skeleton reading from SQLite"
```

---

## Task 14: Scaffold `@vip/worker` skeleton with no-op handlers

**Files:**
- Create: `packages/worker/package.json`
- Create: `packages/worker/tsconfig.json`
- Create: `packages/worker/src/index.ts`
- Create: `packages/worker/src/logger.ts`
- Create: `packages/worker/src/poller.ts`
- Create: `packages/worker/src/jobs/index.ts`
- Create: `packages/worker/src/jobs/noop.ts`

- [ ] **Step 1: Write `packages/worker/package.json`**

```json
{
  "name": "@vip/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@vip/core": "*",
    "drizzle-orm": "^0.36.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
```

- [ ] **Step 2: Write `packages/worker/tsconfig.json`**

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 3: Write `packages/worker/src/logger.ts`**

```ts
export type LogLevel = 'debug' | 'info' | 'warn' | 'error';

export interface Logger {
  info: (msg: string, fields?: Record<string, unknown>) => void;
  warn: (msg: string, fields?: Record<string, unknown>) => void;
  error: (msg: string, fields?: Record<string, unknown>) => void;
  debug: (msg: string, fields?: Record<string, unknown>) => void;
}

function emit(level: LogLevel, msg: string, fields?: Record<string, unknown>): void {
  const entry = { ts: new Date().toISOString(), level, msg, ...fields };
  const line = JSON.stringify(entry);
  if (level === 'error' || level === 'warn') {
    console.error(line);
  } else {
    console.log(line);
  }
}

export const logger: Logger = {
  info: (msg, fields) => emit('info', msg, fields),
  warn: (msg, fields) => emit('warn', msg, fields),
  error: (msg, fields) => emit('error', msg, fields),
  debug: (msg, fields) => emit('debug', msg, fields),
};
```

- [ ] **Step 4: Write `packages/worker/src/jobs/noop.ts`**

```ts
import type { Job } from '@vip/core';
import { logger } from '../logger.js';

export async function noopHandler(job: Job): Promise<void> {
  logger.info('noop handler', { jobId: job.id, kind: job.kind });
}
```

- [ ] **Step 5: Write `packages/worker/src/jobs/index.ts`**

```ts
import type { Job } from '@vip/core';
import { JOB_KINDS, type JobKind } from '@vip/core/types';
import { noopHandler } from './noop.js';

export type JobHandler = (job: Job) => Promise<void>;

export const handlers: Record<JobKind, JobHandler> = Object.fromEntries(
  JOB_KINDS.map((kind) => [kind, noopHandler]),
) as Record<JobKind, JobHandler>;
```

- [ ] **Step 6: Write `packages/worker/src/poller.ts`**

```ts
import { getDb } from '@vip/core/db';
import { jobs } from '@vip/core/schema';
import type { Job, JobKind } from '@vip/core';
import { and, asc, eq, isNull, lte, or, sql } from 'drizzle-orm';
import { handlers } from './jobs/index.js';
import { logger } from './logger.js';

const STUCK_RUNNING_MS = 5 * 60 * 1000;

export async function tick(now: Date = new Date()): Promise<number> {
  const db = getDb();
  const stuckCutoff = new Date(now.getTime() - STUCK_RUNNING_MS);
  db.update(jobs)
    .set({ status: 'queued', started_at: null })
    .where(and(eq(jobs.status, 'running'), lte(jobs.started_at, stuckCutoff)))
    .run();

  const next = db
    .select()
    .from(jobs)
    .where(
      and(
        eq(jobs.status, 'queued'),
        or(isNull(jobs.run_after), lte(jobs.run_after, now)),
      ),
    )
    .orderBy(asc(jobs.created_at))
    .limit(1)
    .all();

  const job = next[0];
  if (!job) return 0;

  db.update(jobs)
    .set({ status: 'running', started_at: now, attempts: sql`${jobs.attempts} + 1` })
    .where(eq(jobs.id, job.id))
    .run();

  try {
    await handlers[job.kind as JobKind](job as Job);
    db.update(jobs)
      .set({ status: 'succeeded', finished_at: new Date(), last_error: null })
      .where(eq(jobs.id, job.id))
      .run();
  } catch (err) {
    const msg = err instanceof Error ? err.stack ?? err.message : String(err);
    logger.error('job failed', { jobId: job.id, kind: job.kind, err: msg });
    db.update(jobs)
      .set({ status: 'failed', finished_at: new Date(), last_error: msg })
      .where(eq(jobs.id, job.id))
      .run();
  }

  return 1;
}

export async function runForever(intervalMs = 1000): Promise<void> {
  logger.info('worker poller started', { intervalMs });
  while (true) {
    const did = await tick();
    if (did === 0) await new Promise((r) => setTimeout(r, intervalMs));
  }
}
```

- [ ] **Step 7: Write `packages/worker/src/index.ts`**

```ts
import { runMigrations } from '@vip/core/migrate.js';
import { runForever } from './poller.js';
import { logger } from './logger.js';

async function main() {
  runMigrations();
  logger.info('worker startup', { node: process.version });
  await runForever();
}

main().catch((err) => {
  logger.error('worker fatal', { err: err instanceof Error ? err.stack : String(err) });
  process.exit(1);
});
```

- [ ] **Step 8: Install + smoke run**

Run: `npm install && npm -w @vip/worker run dev`
Expected: prints two JSON log lines (`worker startup`, `worker poller started`), then sits idle. Stop with Ctrl-C.

- [ ] **Step 9: Commit**

```bash
git add packages/worker/ package.json package-lock.json
git commit -m "feat(worker): job poller skeleton with noop handlers"
```

---

## Task 15: Worker integration test — enqueue → tick → handler ran

**Files:**
- Create: `packages/worker/vitest.config.ts`
- Create: `packages/worker/test/poller.test.ts`

- [ ] **Step 1: Write `packages/worker/vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    pool: 'forks',
    poolOptions: { forks: { singleFork: true } },
  },
});
```

- [ ] **Step 2: Write `packages/worker/test/poller.test.ts`**

```ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { mkdtempSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { runMigrations } from '@vip/core/migrate.js';
import { closeDb, getDb } from '@vip/core/db';
import { jobs } from '@vip/core/schema';
import { eq } from 'drizzle-orm';
import { tick } from '../src/poller.js';
import { handlers } from '../src/jobs/index.js';

let tmp: string;

beforeEach(() => {
  tmp = mkdtempSync(join(tmpdir(), 'vip-worker-test-'));
  process.env.VIP_DB_PATH = join(tmp, 'app.db');
  runMigrations();
});

afterEach(() => {
  closeDb();
  rmSync(tmp, { recursive: true, force: true });
  vi.restoreAllMocks();
});

describe('tick()', () => {
  it('runs the handler for the oldest queued job and marks succeeded', async () => {
    const db = getDb();
    const spy = vi.fn().mockResolvedValue(undefined);
    handlers.send_message = spy;

    db.insert(jobs).values({ kind: 'send_message', payload: { x: 1 } }).run();
    const did = await tick();

    expect(did).toBe(1);
    expect(spy).toHaveBeenCalledOnce();
    const row = db.select().from(jobs).all()[0];
    expect(row?.status).toBe('succeeded');
    expect(row?.attempts).toBe(1);
  });

  it('marks failed and records error when handler throws', async () => {
    const db = getDb();
    handlers.send_message = vi.fn().mockRejectedValue(new Error('boom'));

    db.insert(jobs).values({ kind: 'send_message', payload: {} }).run();
    await tick();

    const row = db.select().from(jobs).all()[0];
    expect(row?.status).toBe('failed');
    expect(row?.last_error).toContain('boom');
  });

  it('respects run_after — does not pick a job scheduled in the future', async () => {
    const db = getDb();
    const future = new Date(Date.now() + 60_000);
    db.insert(jobs).values({ kind: 'check_replies', payload: {}, run_after: future }).run();

    const did = await tick();
    expect(did).toBe(0);
  });

  it('resets stuck running jobs older than 5 minutes', async () => {
    const db = getDb();
    const stale = new Date(Date.now() - 10 * 60 * 1000);
    const row = db
      .insert(jobs)
      .values({ kind: 'check_replies', payload: {}, status: 'running', started_at: stale })
      .returning()
      .get();

    handlers.check_replies = vi.fn().mockResolvedValue(undefined);
    await tick();

    const after = db.select().from(jobs).where(eq(jobs.id, row.id)).get();
    expect(after?.status).toBe('succeeded'); // it got reset to queued, then picked up + ran
  });
});
```

- [ ] **Step 3: Run tests**

Run: `npm -w @vip/worker run test`
Expected: 4 tests pass.

- [ ] **Step 4: Commit**

```bash
git add packages/worker/vitest.config.ts packages/worker/test/poller.test.ts
git commit -m "test(worker): poller dispatch, failure, scheduling, stuck-job reset"
```

---

## Task 16: Verify end-to-end dev experience

**Files:** (none new; manual verification + README updates)

- [ ] **Step 1: From repo root, run the full dev experience**

```
npm install
npm run migrate
npm test
npm run dev
```

Expected:
- `npm test` runs both `@vip/core` (5 tests) and `@vip/worker` (4 tests). Total 9 passing.
- `npm run dev` starts both processes; web at `localhost:3000`, worker logs JSON lines.
- Visit `http://localhost:3000` — page renders "jobs table row count: 0".

- [ ] **Step 2: Enqueue a job manually and watch worker pick it up**

In a second terminal:
```
sqlite3 data/app.db "INSERT INTO jobs (kind, payload) VALUES ('send_message', '{}');"
```

Watch the worker terminal — it should log `noop handler` within a second.

Refresh `localhost:3000` — count should show `1`.

- [ ] **Step 3: Stop dev**

Ctrl-C in the dev terminal.

- [ ] **Step 4: Update README with verified instructions**

Replace the `## Run` section in `README.md` with:

```markdown
## Run

```bash
nvm use            # node 22
npm install
npm run migrate    # initialize SQLite at ./data/app.db
npm run dev        # starts web (localhost:3000) + worker
```

## Test

```bash
npm test           # runs core + worker test suites
```
```

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs: verified run + test instructions"
```

---

## Plan exit criteria

- `npm install` succeeds from clean clone.
- `npm run migrate` creates `data/app.db` with 7 application tables + Drizzle's bookkeeping table.
- `npm test` runs 9 tests across core + worker; all pass.
- `npm run dev` starts both processes; dashboard renders at `localhost:3000` and reads from the DB; worker logs structured JSON.
- Manually inserting a `jobs` row triggers a `noop handler` log line within ~1 second.

When all of the above hold, this plan is done. Move on to Plan 2 (Data Ingest).
